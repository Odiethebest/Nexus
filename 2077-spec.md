# Nexus Dashboard — Cyberpunk 2077 Design Spec v2

## Overview

将 Nexus Dashboard 重新设计为 Cyberpunk 2077 风格的科技朋克 UI。保留全部原有功能，仅替换视觉层。

**本版变更重点**：增大字号、强化光晕效果、引入故障动画、按 HCI 原则重建信息层级。

---

## HCI Principles Applied

**1. Visual Hierarchy（视觉层级）**
- HIGH priority 事件必须在视觉上明显重于 LOW：更大的左侧竖条（6px vs 2px）、粉红光晕、字体加粗
- Topbar 标题最大（20px），面板标题次之（15px），内容标签再次（13px），时间戳最小（12px）——严格四级层级

**2. Affordance（可操作性）**
- Publish 按钮占满左栏宽度，高度 `52px`，视觉重量明确传达"这是主操作"
- 输入框 focus 状态光晕范围扩大到 `12px`，让用户清晰感知当前焦点

**3. Feedback（操作反馈）**
- Publish 后按钮短暂变为 `[ TRANSMITTED ]` 并闪烁一次，300ms 后恢复
- 新通知卡片出现时带故障闪烁（glitch）动画，强化"数据传入"感

**4. Readability（可读性）**
- 最小字号 12px（时间戳），正文 14px，标题 15px+
- 行高 `1.6`，卡片内容区 padding 不小于 `14px`
- 强调色文字必须在深色背景上对比度 ≥ 4.5:1

---

## Color System

| Token | Value | Usage |
|-------|-------|-------|
| `--bg-base` | `#080809` | 页面背景（比之前更深） |
| `--bg-panel` | `#0e0e11` | 面板背景 |
| `--bg-input` | `#16161a` | 输入框背景 |
| `--accent-yellow` | `#f5e642` | 主强调色 |
| `--accent-cyan` | `#00d4ff` | 次强调色 |
| `--accent-pink` | `#ff2d6b` | 危险/HIGH priority |
| `--text-primary` | `#f0f0e8` | 正文（提亮，增强对比度） |
| `--text-secondary` | `#8a8a80` | 次要信息 |
| `--border-default` | `rgba(245,230,66,0.2)` | 默认面板边框 |
| `--border-input` | `rgba(0,212,255,0.5)` | 输入框边框（加深） |

### Glow Values（光晕，按强度分级）

```css
--glow-yellow-sm: 0 0 8px rgba(245,230,66,0.4);
--glow-yellow-md: 0 0 20px rgba(245,230,66,0.5);
--glow-cyan-sm:   0 0 8px rgba(0,212,255,0.4);
--glow-cyan-md:   0 0 20px rgba(0,212,255,0.5);
--glow-pink-sm:   0 0 8px rgba(255,45,107,0.5);
--glow-pink-md:   0 0 24px rgba(255,45,107,0.6);
```

---

## Typography

- 全局字体：`'Share Tech Mono', monospace`（从 Google Fonts 加载，比系统 monospace 更 cyberpunk）
- 备用：`'Courier New', monospace`
- **字号层级（严格四级）**：

| 层级 | 元素 | Size | Weight |
|------|------|------|--------|
| L1 | Topbar 标题 | 20px | 700 |
| L2 | 面板标题 | 15px | 600 |
| L3 | 字段 label、正文 | 13px | 500 |
| L4 | 时间戳、次要信息 | 12px | 400 |

- 所有 label、panel title 全部大写，`letter-spacing: 0.12em`
- 行高统一 `1.6`

---

## Layout

双栏 Grid，比例 `1fr : 1.6fr`，两栏等高撑满内容区。

```
┌─────────────────────────────────────────────────┐
│ ▋ NEXUS DASHBOARD              ■ DISCONNECTED   │  ← topbar，底部霓虹线
├──────────────────┬──────────────────────────────┤
│ ╱ PUBLISH EVENT  │ ╱ LIVE NOTIFICATIONS  0 EVT  │
│                  │                              │
│  TYPE            │   ┌──────────────────────┐   │
│  [         ]     │   │▌order  [HIGH] SYS:2m │   │
│                  │   │ {"user_id":"u123"}    │   │
│  PRIORITY  [HIGH]│   └──────────────────────┘   │
│  [      ▼  ]     │                              │
│                  │   Waiting for events...       │
│  PAYLOAD (JSON)  │                              │
│  [         ]     │                              │
│                  │                              │
│  [ PUBLISH ]     │                              │
└──────────────────┴──────────────────────────────┘
```

---

## Components

### Topbar

```css
background: #0e0e11;
border-bottom: 1px solid #f5e642;
box-shadow: 0 1px 16px rgba(245,230,66,0.5), 0 2px 40px rgba(245,230,66,0.15);
height: 56px;
padding: 0 28px;
```

标题加文字光晕：
```css
color: #f5e642;
text-shadow: 0 0 12px rgba(245,230,66,0.8), 0 0 30px rgba(245,230,66,0.3);
```

### Panel

```css
background: #0e0e11;
clip-path: polygon(14px 0%, 100% 0%, 100% 100%, 0% 100%, 0% 14px);
border: 1px solid rgba(245,230,66,0.25);
border-left: 3px solid #f5e642;
box-shadow: inset 0 0 40px rgba(245,230,66,0.03);  /* 内发光，增加深度 */
padding: 24px;
```

面板标题加扫描线前缀 `/ `，颜色 `#f5e642`，`text-shadow: 0 0 8px rgba(245,230,66,0.6)`

### Input / Select / Textarea

```css
background: #16161a;
border: 1px solid rgba(0,212,255,0.5);
border-radius: 0;
color: #f0f0e8;
font-family: inherit;
font-size: 14px;
padding: 10px 14px;
height: 42px;  /* textarea 除外 */

:focus {
  border-color: #00d4ff;
  box-shadow: 0 0 12px rgba(0,212,255,0.4), inset 0 0 8px rgba(0,212,255,0.05);
  outline: none;
}
```

### Publish Button

高度 `52px`，宽度 `100%`，主操作权重最高：

```css
background: #f5e642;
color: #080809;
font-size: 15px;
font-weight: 700;
letter-spacing: 0.15em;
height: 52px;
border: none;
border-radius: 0;
text-transform: uppercase;
cursor: pointer;
transition: box-shadow 0.15s;

:hover {
  box-shadow: 0 0 20px rgba(245,230,66,0.6), 0 0 50px rgba(245,230,66,0.2);
}
:active { transform: scale(0.99); }
```

**操作反馈**：点击后文字切换为 `[ TRANSMITTED ]`，按钮背景闪烁一次（`#fff` → `#f5e642`），300ms 后恢复原文字。

### Priority Badge

```css
background: transparent;
border: 1px solid <priority-color>;
color: <priority-color>;
font-size: 12px;
font-weight: 700;
letter-spacing: 0.1em;
padding: 3px 8px;
border-radius: 0;
text-shadow: 0 0 6px <priority-color>;
box-shadow: 0 0 6px <priority-color-alpha>;  /* 外发光 */
```

| Priority | Color | Glow |
|----------|-------|------|
| HIGH | `#ff2d6b` | `0 0 8px rgba(255,45,107,0.6)` |
| MEDIUM | `#f5e642` | `0 0 8px rgba(245,230,66,0.5)` |
| LOW | `#00d4ff` | `0 0 8px rgba(0,212,255,0.5)` |

### Notification Card

左侧竖条宽度随 priority 变化，高 priority 更粗：

| Priority | 竖条宽度 | 竖条颜色 | 卡片背景 |
|----------|---------|---------|---------|
| HIGH | `6px` | `#ff2d6b` | `rgba(255,45,107,0.05)` |
| MEDIUM | `4px` | `#f5e642` | `rgba(245,230,66,0.03)` |
| LOW | `2px` | `#00d4ff` | `transparent` |

HIGH priority 卡片额外加右侧粉红光晕：
```css
box-shadow: inset -2px 0 20px rgba(255,45,107,0.08);
```

**Glitch 入场动画**（替换原 slideIn）：

```css
@keyframes glitch-in {
  0%   { opacity: 0; transform: translateX(-4px); clip-path: inset(30% 0 50% 0); }
  20%  { opacity: 0.8; transform: translateX(2px); clip-path: inset(0% 0 0% 0); }
  40%  { transform: translateX(-1px); }
  60%  { transform: translateX(0); clip-path: inset(10% 0 20% 0); }
  80%  { clip-path: inset(0% 0 0% 0); }
  100% { opacity: 1; transform: translateX(0); }
}
animation: glitch-in 0.25s ease forwards;
```

时间戳格式：`SYS_TIME: Xm` 颜色 `#8a8a80`

卡片内 padding：`14px 18px`，行高 `1.6`

### Connection Status

方块图标 `■`，字号 `13px`：

- connected：`#f5e642`，`text-shadow: 0 0 8px rgba(245,230,66,0.8)`
- disconnected：`#ff2d6b`，加闪烁：

```css
@keyframes blink {
  0%, 100% { opacity: 1; }
  50%       { opacity: 0.15; }
}
animation: blink 1s ease infinite;
```

---

## Animation Summary

| 元素 | 动画 | 时长 |
|------|------|------|
| 通知卡片入场 | `glitch-in`：故障闪烁 + 位移 | 0.25s |
| Publish 按钮反馈 | 文字切换 + 背景闪白 | 300ms |
| disconnected 状态 | `blink`：透明度脉冲 | 1s infinite |
| 输入框 focus | box-shadow 过渡 | 0.15s |
| 按钮 hover | glow 扩散 | 0.15s |

---

## Font Loading

```html
<link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap" rel="stylesheet">
```

---

## What stays the same

- 双栏布局结构（`1fr : 1.6fr`）
- 表单字段：Type、Priority、Payload
- Publish 功能逻辑
- 通知列表渲染逻辑（新事件插入顶部，带计数）
- WebSocket 接入接口